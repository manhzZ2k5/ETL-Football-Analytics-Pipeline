[postgresql]
# PostgreSQL đã có sẵn trên máy local
# host.docker.internal: Dùng khi chạy từ Docker container (Windows/Mac)
# localhost: Dùng khi chạy trực tiếp trên máy (không qua Docker)
# Nếu Linux, có thể cần dùng IP thật của host (vd: 192.168.1.100)
host=localhost
database=ETL_FOOTBALL
user=football_user
password=12345
port=5432